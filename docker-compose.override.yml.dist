version: '3'

services:
  nginx:
    ports:
      - 80:80

  php:
    volumes:
      - .:/var/www/jiraboard

  mysql:
    ports:
      - 3306:3306
    environment:
    - MYSQL_ROOT_PASSWORD=root-passwd
    - MYSQL_DATABASE=db-name
    - MYSQL_USER=db-user
    - MYSQL_PASSWORD=db-user-passwd
