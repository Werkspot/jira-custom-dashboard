{% extends 'base.html.twig' %}

{% block body %}
    <div style="margin: 15px; width: 300px; height: 300px; border: 1px solid blue;">
        Add a new Sprint...
        {% if error is not empty %}
            {{ error }}
        {% endif %}
        {{ form(form) }}
    </div>
{% endblock %}