<div style="margin: 15px; width: 300px; height: 300px; border: 1px solid blue;">

    {% for message in app.flashes('confidence') %}
        <div style="color: red">{{ message }}</div>
    {% endfor %}

    Add a new Confidence

    {% if error is not empty %}
        {{ error }}
    {% endif %}
    
    {{ form(form) }}
</div>
